import express from"express";import{fileURLToPath}from"url";import{dirname,join}from"path";let __filename=fileURLToPath(import.meta.url),__dirname=dirname(__filename),app=express(),port=process.env.PORT||3e3;app.use(express.static(join(__dirname,"dist"))),app.use((e,r)=>{r.sendFile(join(__dirname,"dist","index.html"))}),app.listen(port,()=>{console.log("Server running at http://localhost:"+port)});